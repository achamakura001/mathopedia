# Frontend Environment Configuration

# API Base URL Configuration
# Choose one of the following based on your deployment scenario:

# 1. Development (when backend runs on localhost:5000)
REACT_APP_API_URL=http://localhost:5000

# 2. Production with different server/port
# REACT_APP_API_URL=http://your-server-ip:5000
# REACT_APP_API_URL=https://your-domain.com

# 3. Same-host deployment (backend and frontend on same server/port)
# Leave REACT_APP_API_URL commented out or empty - app will use relative URLs
# REACT_APP_API_URL=

# 4. Docker or containerized deployment
# REACT_APP_API_URL=http://backend:5000

# Note: If REACT_APP_API_URL is not set, the app will:
# - Use relative URLs in production (good for same-host deployments)
# - Fall back to http://localhost:5000 in development
